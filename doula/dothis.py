# create a parent view class
# create sub classes and break up the views so that it makes sense.
# figure out how to create classes properly to break things up

# start running jobs on the queue that populate the queue
# start pulling jobs off the queue
# start running queries that populate redis
# start pulling data from the queue, package to git commit needs to be made
# do that
